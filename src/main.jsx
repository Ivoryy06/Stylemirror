import React from "react";
import ReactDOM from "react-dom/client";
import StyleMirror from "../StyleMirror.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StyleMirror />
  </React.StrictMode>
);
